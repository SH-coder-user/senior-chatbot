# PostgreSQL 데이터베이스 설정
DB_USER=postgres
DB_HOST=localhost
DB_NAME=senior_chatbot
DB_PASSWORD=postgres
DB_PORT=5432

# 서버 설정
PORT=5000
NODE_ENV=development

# CORS 설정 (필요시)
ALLOWED_ORIGINS=http://localhost:3000

# 주의: 이 파일은 .gitignore에 추가하여 Git에 업로드하지 마세요!